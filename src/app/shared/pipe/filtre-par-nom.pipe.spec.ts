import { FiltreParNomPipe } from './filtre-par-nom.pipe';

describe('FiltreParNomPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreParNomPipe();
    expect(pipe).toBeTruthy();
  });
});
